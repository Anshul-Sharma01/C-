#include <iostream>     // Pre processor directive
using namespace std;

#define PI 3.14

// Macro is a piece of code in a program that is replaced by value of macro
// We don't take any extra storage on declaring the macro
// Global Variables : 
// Local Variables : 

int score = 15;  // Not recommended and considered to be bad programming practice

// Inline Functions : They are used to reduce the function calls overhead

int main(){
    cout<<PI;
    return 0;
}
